# 블로그 글쓰기 앱 — 기능 요구사항 명세

- 버전: v0.1 (초안)
- 작성일: 2026-08-28
- 플랫폼: Android (갤럭시 탭), WebView 기반 네이버 블로그 발행
- 디자인/UX 방향성: Toss-like (본 문서에서는 다루지 않고 별도 문서로 분리)

---

## 1. 개요

사진과 대략적인 줄거리 및 요구사항을 입력하면 LLM API가 완성된 블로그 글을 생성하고, 앱 내 WebView를 통해 네이버 블로그 스마트에디터에 자동 입력한 뒤, 사용자가 최종 확인과 발행만 수행하는 Android 앱이다.

### 1.1 사용자 역할

| 역할 | 설명 |
|---|---|
| 사용자 | 글 작성, 검토, 발행을 수행하는 주 사용자 |
| 관리자 | 초기 설정, API 키 관리, 스타일 프로필 관리, 문제 해결 담당 |

### 1.2 외부 의존성

- Google Gemini API (Gemini 3.7 Flash, 무료 티어): 글 생성, 사진 분석, 스타일 분석
- 네이버 블로그 스마트에디터 (WebView): 글 발행
- 네이버 블로그: 기존 글 수집 (글 스타일 파악 및 더 나은 글쓰기 조언)

---

## 2. 기능 요구사항

### FR-1. 초기 설정 및 인증

| ID | 요구사항 |
|---|---|
| FR-1.1 | 앱 내 WebView로 네이버 로그인을 수행하고, 세션 쿠키를 `CookieManager`에 영속 저장한다. |
| FR-1.2 | 로그인 성공 후 블로그 ID를 자동으로 추출한다. |
| FR-1.3 | 세션 만료를 감지하면 재로그인 화면을 안내하고, 진행 중이던 작업(생성된 글)은 로컬에 보존했다가 로그인 완료 후 중단 지점부터 재개한다. |
| FR-1.4 | 사용자가 자력으로 해결하기 어려운 상황을 위해 "관리자에게 알리기" 기능을 제공한다. |

### FR-2. API 키 관리 (관리자)

| ID | 요구사항 |
|---|---|
| FR-2.1 | 설정 화면에서 Gemini API 키를 여러 개 등록·삭제할 수 있다. |
| FR-2.2 | 키는 Android Keystore 기반으로 암호화하여 기기 내에만 저장한다. 외부 서버로 전송하지 않는다. |
| FR-2.3 | 키 등록 시 유효성 테스트(테스트 호출)를 수행하고 결과를 표시한다. |
| FR-2.4 | API 호출이 429(한도 초과)로 실패하면 등록된 다음 키로 자동 전환하여 재시도한다. 모든 키가 소진되면 사용자에게 잠시 후 재시도를 안내한다. |
| FR-2.5 | 설정 화면 진입은 관리자 전용 잠금(PIN 등)으로 보호하여 사용자의 오조작을 방지한다. |
| FR-2.6 | Gemini API는 모델별로도 RPM, RPD가 다르기 때문에 모든 키 사용에 실패했다면 다음 모델 (예를 들자면 3.7-flash -> 3-5.flash-lite)로 다운그레이드해서 시도한다. (일시적 변경, 3.7 사용 가능해지면 다시 원복)

### FR-3. 스타일 프로필

| ID | 요구사항 |
|---|---|
| FR-3.1 | 온보딩 시 블로그에서 글을 수집하고, LLM으로 스타일 프로필을 추출한다. 프로필 항목: 말투, 문단 길이, 이모티콘 사용 빈도, 자주 쓰는 표현, 사진 배치 습관. |
| FR-3.2 | 스타일 프로필은 사용자가 직접 읽고 수정할 수 있어야 한다. |
| FR-3.4 | 스타일 프로필은 모든 글 생성 요청의 프롬프트에 반영된다. |
| FR-3.5 | 사용자의 글들을 (특정 글들을 선택 가능) 읽고 조언을 제공해야 한다 |

### FR-4. 글 작성 입력

| ID | 요구사항 |
|---|---|
| FR-4.1 | 갤러리에서 사진을 다중 선택할 수 있다. 선택된 사진은 촬영 시각 순으로 자동 정렬하되 수동으로 순서를 변경할 수 있다. |
| FR-4.2 | 줄거리를 텍스트로 입력할 수 있다. 짧은 메모 수준의 입력을 전제로 한다. |
| FR-4.3 | 음성 입력(Android STT)으로 줄거리를 받아쓸 수 있다. |
| FR-4.4 | 작성 중인 입력(사진 선택 상태, 줄거리)은 앱이 중단되어도 유실되지 않도록 지속적으로 자동 임시 저장한다. |

### FR-5. 글 생성

| ID | 요구사항 |
|---|---|
| FR-5.1 | 사진(리사이즈 후 vision 입력), 줄거리, 스타일 프로필을 LLM에 전달하여 완성된 글을 생성한다. 사진의 내용이 본문 서술에 반영되어야 한다. |
| FR-5.2 | 제목 후보를 2~3개 생성하여 선택지로 제시한다. |
| FR-5.3 | 본문은 사진이 문단 사이에 배치된 완성 형태로 생성한다. 사진 삽입 위치는 생성 결과에 마커로 포함한다. |
| FR-5.4 | 본문은 스마트에디터가 수용 가능한 표준 HTML 서식(굵게, 글자 크기·색, 정렬, 목록, 인용구)으로 생성한다. |
| FR-5.5 | 생성 실패(네트워크, API 오류) 시 입력을 보존한 채 재시도할 수 있다. |

### FR-6. 검토 및 부분 수정

| ID | 요구사항 |
|---|---|
| FR-6.1 | 생성 결과를 실제 발행 형태에 가까운 미리보기로 표시한다. |
| FR-6.2 | 문단 단위로 선택하여 재생성을 요청할 수 있다. 최소 지원 지시: 다시 쓰기, 더 짧게, 더 길게. |
| FR-6.3 | 본문 텍스트를 직접 수정할 수 있다. |
| FR-6.4 | 검토 중인 글은 초안으로 저장되며, 나중에 이어서 작업할 수 있다. |

### FR-7. 발행 (WebView 자동 입력)

| ID | 요구사항 |
|---|---|
| FR-7.1 | WebView로 스마트에디터 글쓰기 페이지를 로드하고, 제목을 자동 입력한다. |
| FR-7.2 | 본문 HTML은 `text/html`을 담은 합성 paste 이벤트로 주입하여 서식을 유지한다. |
| FR-7.3 | 사진 첨부는 `onShowFileChooser` 콜백을 가로채서 파일 선택 UI 없이 지정된 순서대로 업로드한다. |
| FR-7.4 | 자동 입력 진행 중에는 진행 상태 화면으로 에디터 조작 과정을 가린다. |
| FR-7.5 | 자동 입력 완료 후 WebView를 노출하여 사용자가 내용을 확인하고 에디터의 발행 버튼을 직접 탭한다. 발행 자체는 자동화하지 않는다. |
| FR-7.6 | 발행 완료 URL을 감지하여 발행 이력에 기록한다. |

더 나은 방식이 있다면 대체 가능

### FR-8. 발행 실패 폴백

| ID | 요구사항 |
|---|---|
| FR-8.1 | 자동 입력이 제한 시간 내에 완료 신호를 보내지 않으면 실패로 판정한다. |
| FR-8.2 | 실패 시 완성 글 전체를 클립보드에 복사하고, 네이버 블로그 앱을 여는 폴백 경로와 붙여넣기 안내를 제공한다. |
| FR-8.3 | 실패 발생 사실과 원인 정보(에디터 구조 변경 추정 등)를 관리자가 확인할 수 있게 기록한다. |

### FR-9. 블로그 진단 (스타일 피드백)

| ID | 요구사항 |
|---|---|
| FR-9.1 | RSS로 최근 글을 수집하여 글별 개선 피드백을 생성한다. 피드백 관점: 제목의 검색 노출, 문단 나누기, 사진 배치, 도입부. |
| FR-9.2 | 전체 글의 경향 요약을 제공한다. |
| FR-9.3 | 사용자가 선택한 개선 제안을 스타일 프로필에 병합하여 다음 글 생성부터 반영한다. |

### FR-10. 이력 관리

| ID | 요구사항 |
|---|---|
| FR-10.1 | 발행 이력(제목, 발행 일시, 블로그 URL)을 목록으로 제공한다. |
| FR-10.2 | 초안 목록을 제공하며, 초안에서 검토 화면으로 바로 진입할 수 있다. |
| FR-10.3 | 이력 항목에서 해당 블로그 글을 브라우저로 열 수 있다. |

---

## 3. 제약 및 전제

| ID | 내용 |
|---|---|
| C-1 | 네이버 블로그 공식 글쓰기 API는 종료되었으므로(2020.05) WebView 자동 입력 방식으로 우회하되, 최종 발행은 사용자가 수행한다. |
| C-2 | Gemini 무료 티어 한도(5 RPM / 250K TPM / 20 RPD, 프로젝트별 상이 가능)를 전제로 설계한다. 예상 사용량은 한도 대비 충분히 낮다. |
| C-3 | 스마트에디터는 비공식 연동 대상이므로 구조 변경 시 FR-7이 동작하지 않을 수 있다. FR-8 폴백이 항상 유효해야 한다. |
| C-4 | 저빈도 사용(일 1~2건 발행)을 전제로 하며, 대량·상업적 자동 발행은 지원 범위가 아니다. |

## 4. 범위 제외

- 완전 자동 발행(발행 버튼 자동화)
- 네이버 고유 컴포넌트(스티커, 장소·링크 카드, 특수 인용구) 삽입 — 추후 검토
- 다중 블로그 계정 지원
- iOS 지원

---

## 5. 변경 이력

### 2026-08-28 — 글 작성 흐름을 채팅형으로 (FR-4~FR-7 보완)

- 글 작성은 ChatGPT 류의 **채팅 UI**로 진행한다: 사용자가 사진과 아이디어를 주면 LLM이 "이렇게 써 볼까요?" 형식의 계획을 제안하고, 2~3회 상호작용으로 다듬은 뒤, 사용자가 "초안 작성"을 요청한 시점에만 스마트에디터에 입력한다.
- 초안 작성 시 화면 오른쪽에 WebView 사이드 패널이 열려 자동 입력 과정과 에디터를 보여준다. 채팅은 왼쪽에 유지되어 부분 수정 요청을 계속 받는다.
- FR-7 구현 방식은 스파이크 결과(`spike/findings.md`)에 따라 합성 paste/`onShowFileChooser` 대신 에디터 내부 API(`setDocumentData`, 이미지 업로드 서비스)를 사용한다. FR-5.4의 "HTML 서식" 은 앱 내부 블록 모델(`PostContent`)로 대체한다.
- 상세는 `docs/design-guide.md` §8, `docs/superpowers/specs/`.

